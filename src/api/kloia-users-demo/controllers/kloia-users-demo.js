'use strict';

/**
 * kloia-users-demo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::kloia-users-demo.kloia-users-demo');
